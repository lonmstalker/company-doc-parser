logging:
  level:
    root: ERROR
    com.zuzex: debug

app:
  properties:
    archive-folder: archives-mini

spring:
  datasource:
    url: jdbc:postgresql://${DATASOURCE_URL:localhost:5432}/parser
    username: ${DATASOURCE_USERNAME:parser}
    password: ${DATASOURCE_PASSWORD:parser}
    driver-class-name: org.postgresql.Driver